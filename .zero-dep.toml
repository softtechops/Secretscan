track = "E"
pitch = "A zero-dependency local secrets scanner that catches hardcoded API keys, tokens, and high-entropy strings before they get committed — using only Python's standard library."
language = "Python 3.14"
